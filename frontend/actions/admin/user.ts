export async function userGetDashboardStats() {

    const totalEnrollments = 0;
    const totalPathsEnrollments = 0;
    const totalCoursesEnrollments = 0;
    const totalWorkshopsEnrollments = 0;

    return {
        totalEnrollments,
        totalPathsEnrollments,
        totalCoursesEnrollments,
        totalWorkshopsEnrollments,
    }
}