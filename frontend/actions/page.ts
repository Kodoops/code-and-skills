import {Page} from "@/models";

export async function getPageLinks(type: string): Promise<Page[]> {

    return [];
}
/*
data: {
    id: string
    createdAt: Date
    updatedAt: Date
    title: string
    slug: string
    type: string
    content: string
}
[]
*/
